apiVersion: v1
kind: ConfigMap
metadata:
  name: template-agent-config
  labels:
    app: template-agent
    component: agent
data:
  PYTHON_LOG_LEVEL: "INFO"
  LANGFUSE_TRACING_ENVIRONMENT: "production"
