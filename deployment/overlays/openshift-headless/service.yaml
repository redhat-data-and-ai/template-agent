apiVersion: v1
kind: Service
metadata:
  name: agent-headless
  labels:
    app: agent
    component: agent-headless
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: health
  selector:
    app: agent
    component: agent-headless
