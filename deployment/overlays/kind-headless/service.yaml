apiVersion: v1
kind: Service
metadata:
  name: agent-headless
  labels:
    app: agent
    component: agent-headless
spec:
  selector:
    app: agent
    component: agent-headless
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: health
  type: ClusterIP
