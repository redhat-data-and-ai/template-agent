apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: template-agent

resources:
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      app: template-agent
      component: agent-headless

images:
  - name: agent
    newName: localhost/template-agent
    newTag: local
