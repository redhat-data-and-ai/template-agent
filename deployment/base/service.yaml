apiVersion: v1
kind: Service
metadata:
  name: agent
  labels:
    app: agent
    component: agent
spec:
  type: ClusterIP
  ports:
    - port: 5002
      targetPort: 5002
      protocol: TCP
      name: http
  selector:
    app: agent
    component: agent
